function addShadow(icon) {
    icon.style.boxShadow = "0 0 10px rgba(0, 0, 0, 0.5)"; // Adiciona sombra ao passar o mouse
  }
  
  function removeShadow(icon) {
    icon.style.boxShadow = "none"; // Remove sombra ao retirar o mouse
  }  
  
